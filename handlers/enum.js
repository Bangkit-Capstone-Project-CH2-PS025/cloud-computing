const ROLE = {
  ADMIN: "ADMIN",
  USER: "USER",
};

const VERIFIED = {
  TRUE: true,
  FALSE: false,
};

const LEVEL_TRAVELLER = {
  NEWBIE: 1,
  SEASONAL: 2,
  TRAVELLER: 3,
  EXPLORER: 4,
};

module.exports = { ROLE, VERIFIED, LEVEL_TRAVELLER };
